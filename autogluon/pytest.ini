# pytest.ini
[pytest]
# Tests import `project.app`; bare `pytest` (as CI runs it) would not otherwise
# put the rootdir on sys.path the way `python -m pytest` does.
pythonpath = .
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')